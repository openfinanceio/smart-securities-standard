import BigNumber from "bignumber.js";
import * as Web3 from "web3";

describe("ARegD506cToken", function() {

  it("should construct the contract");
  it("should set up the cap table");
  it("should fail to transfer after issuing (before finish)");
  it("should fail to transfer without AML KYC");
  it("should fail to transfer without accredited buyer");
  it("should fail to transfer if it would exceed shareholder limit");

});
