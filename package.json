{
  "name": "smart-securities-standard",
  "version": "0.1.1",
  "license": "MIT",
  "scripts": {
    "compile": "ls contracts/*.sol | xargs solc --optimize --abi -o build --overwrite",
    "test": "tsc && mocha dist/test"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.48",
    "mocha": "^5.0.1",
    "web3-typescript-typings": "^0.9.11"
  },
  "dependencies": {
    "bignumber.js": "^6.0.0",
    "web3": "^0.20.4"
  }
}
